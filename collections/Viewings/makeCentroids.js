export default function makeCentroids(viewing) {
  // period = viewing.analysis().period;
  // recordings = viewing.recordings().fetch();

  // hulls = viewing.getHulls(viewing);

  // console.log('balls:');
  // console.log(hulls.length);

  // console.log('make centroids');
};
