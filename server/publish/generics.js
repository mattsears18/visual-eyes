////////////////////////////////////////////////////////////////////////////////
// Generics Publications
////////////////////////////////////////////////////////////////////////////////
Meteor.publish('generics.all', function() {
  return Generics.find({});
});

Meteor.publish('generics.single', function(id) {
  check(id, String);
  return Generics.find({_id: id});
});
